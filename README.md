# 🧩 FastAPI Accounts & Goods Service

## 🎯 Цели проекта

Проект представляет собой REST API-сервис для управления пользователями (**accounts**) и товарами (**goods**).

Основные задачи:
- регистрация и хранение данных пользователей;
- управление товарами (добавление, получение, редактирование, удаление);
- взаимодействие с PostgreSQL через SQLAlchemy ORM;
- простая интеграция и развёртывание через **Docker Compose**.

---

## ⚙️ Команды для запуска

### 🚀 Запуск через Docker
```bash
# Сборка и запуск контейнеров
docker compose up --build 
```
# После запуска сервис будет доступен по адресу:
http://localhost:8000

## Архитектура
```
app/
│
├── .env
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
│
└── init/
│   └── init.sql
│
└── src/
    ├── main.py
    ├── api/
    │   ├── accounts_api.py
    │   └── goods_api.py
    ├── models/
    │   ├── base.py
    │   ├── accounts.py
    │   └── goods.py
